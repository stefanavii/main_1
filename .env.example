# Copy this file to ".env" and fill in your real MySQL password.
# This file (.env.example) is just a template — it is safe to share.
# The real ".env" file should never be shared, since it has your password.

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=PUT_YOUR_MYSQL_PASSWORD_HERE
DB_NAME=agent_vacancy_db
PORT=3000
